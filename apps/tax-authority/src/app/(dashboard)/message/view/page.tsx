"use client";
import ViewMessage from "@/pattern/clients/templates/view-message";

const Manage = () => {
	return (
		<div className='w-full min-h-full h-fit flex flex-col gap-y-[144px] mb-[144px]'>
			<ViewMessage />
		</div>
	);
};

export default Manage;

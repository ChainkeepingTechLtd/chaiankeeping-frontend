"use client";

import ManageClient from "@/pattern/clients/templates/manage-client";

const Manage = () => {
	return (
		<div className='w-full min-h-full h-fit flex flex-col gap-y-[144px] mb-[144px]'>
			<ManageClient />
		</div>
	);
};

export default Manage;
